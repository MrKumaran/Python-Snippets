Basic Python exception handling

Lot of times exceptions can occur which may crash our entire program, inorder  to prevent that
from happening we use exception handling

some inbuilt errors:
    ->SyntaxError - syntax mistakes,
    ->ZeroDivisionError - When attempt to divide a number by 0,
    ->NameError - if Variable name, function name,Class name is not declared this error arises,
    ->TypeError - data type error.

In this Example TypeError arises